# Manual Review Queue

Generated: 2026-08-27 13:49:24
Project: `demo-villa-simulated`
Run iteration: 2

These findings require human judgment. They were flagged because the rule has `requires_human: true` AND the value failed the deterministic check. Review each item and either resolve in the model, grant a derogation, or escalate.

> **Scope note.** QCAgent always evaluates *every* rule in the active YAML; the operator's `--rule` filter only scopes the subsequent DesignAgent (ACC issues + Path B writes). This queue reflects the full QC pass, so rows here may include rule IDs that aren't covered by the current `--rule` filter -- intentional, since manual-review items are not auto-routed anywhere.

## Summary

No manual-review items in this run.
